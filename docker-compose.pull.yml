services:
  open-webui:
    image: ${OPEN_WEBUI_IMAGE:-pinkmoone/open_webui}:${OPEN_WEBUI_TAG:-v1.3}
    container_name: open-webui
    pull_policy: always
    restart: unless-stopped
    ports:
      - "${OPEN_WEBUI_PORT:-3000}:8080"
    volumes:
      - ${OPEN_WEBUI_DATA_DIR:-open-webui}:/app/backend/data
    environment:
      # If Ollama is started by this compose file, keep this value.
      # If Ollama runs on the Docker host, use: http://host.docker.internal:11434
      # If Ollama runs on another machine, use: http://SERVER_IP:11434
      OLLAMA_BASE_URL: ${OLLAMA_BASE_URL:-http://ollama:11434}

      # Optional OpenAI or OpenAI-compatible API.
      OPENAI_API_BASE_URL: ${OPENAI_API_BASE_URL:-}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}

      # Set a stable random value before first production use.
      WEBUI_SECRET_KEY: ${WEBUI_SECRET_KEY:-}

      # Optional opening-screen verification code. Empty disables it.
      WEBUI_OPENING_CODE: ${WEBUI_OPENING_CODE:-}
      WEBUI_OPENING_CODE_EXPIRES_MINUTES: ${WEBUI_OPENING_CODE_EXPIRES_MINUTES:-10}

      # Privacy / telemetry switches.
      SCARF_NO_ANALYTICS: ${SCARF_NO_ANALYTICS:-true}
      DO_NOT_TRACK: ${DO_NOT_TRACK:-true}
      ANONYMIZED_TELEMETRY: ${ANONYMIZED_TELEMETRY:-false}
    extra_hosts:
      - host.docker.internal:host-gateway

  ollama:
    image: ollama/ollama:${OLLAMA_DOCKER_TAG:-latest}
    container_name: ollama
    pull_policy: always
    restart: unless-stopped
    tty: true
    volumes:
      - ${OLLAMA_DATA_DIR:-ollama}:/root/.ollama
    profiles:
      - ollama

volumes:
  open-webui: {}
  ollama: {}
